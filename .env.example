# Postgres connection for production. Leave unset to use the embedded PGlite database under ./data.
DATABASE_URL=postgres://user:pass@host:5432/breezevibe
# Optional: where PGlite stores its files when DATABASE_URL is unset.
PGLITE_DIR=./data/pglite
